# Всё, что собирается или снимается на месте, в репозиторий не едет.
out/
bin/
obj/
baseline/
.alsrc/

# Выгрузки объектов целевой базы и рабочие копии в cp866.
*.cp866.txt
*.bak

# Секреты - только вне репозитория, но пусть сторож стоит и здесь.
.env
.key
.secrets/

# Конкретика стенда: имена баз, компаний и объектов заказчика - только вне git.
local/